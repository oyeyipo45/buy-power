AuctionsTableIAM:
  Effect: 'Allow'
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
  Resource:
    - ${self:custom.AuctionsTable.arn}
CALEB:
  Effect: 'Allow'
  Action:
    - dynamodb:PutItem
    - dynamodb:Scan
    - dynamodb:GetItem
  Resource:
    - ${self:custom.AuctionsTable.arn}